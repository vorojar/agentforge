# Database (SQLite)
# DATABASE_URL is still accepted for compatibility, but DB_PATH is preferred.
DB_PATH=data/agentforge.db

# LLM Provider
LLM_PROVIDER=claude
LLM_API_KEY=
# LLM_BASE_URL=  # Optional: for OpenAI-compatible providers
# DEFAULT_MODEL=claude-sonnet-4-20250514

# Server
PORT=3000
ADMIN_SECRET=change-me-in-production
ADMIN_EMAIL=demo@example.com
# Local demo password only. Production refuses this value.
ADMIN_PASSWORD=password
AUTH_SESSION_DAYS=7
